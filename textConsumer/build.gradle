group 'com.halushko.kinocat.textConsumer'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'application'

compileJava.options.encoding = 'UTF-8'

jar {
    manifest {
        attributes 'Main-Class': 'com.halushko.kinocat.textConsumer.Main'
    }
}

artifacts {
    archives jar
}

dependencies {
    implementation 'com.rabbitmq:amqp-client:+'
    implementation 'org.json:json:+'
    implementation 'org.slf4j:slf4j-log4j12:+'
    implementation files(System.getenv('my_dir') + "/core/build/libs/core-1.0-SNAPSHOT.jar")
}

repositories {
    mavenCentral()
}